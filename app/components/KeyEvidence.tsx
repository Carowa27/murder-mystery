'use client';
import { KeyIcon } from '@phosphor-icons/react';

export const KeyEvidence = () => {
  return (
    <div className="absolute bottom-2 w-full flex items-center justify-center">
      <KeyIcon size={25} color="#000000" weight="duotone" className="" />
    </div>
  );
};
